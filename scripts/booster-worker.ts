import{loadAuthoritativeEnvironment}from"../lib/server/production-environment";import{getPool}from"../lib/server/db";import{runBoosterScheduler,withBoosterWorkerLock}from"../lib/server/booster-service";import{isModulePaused}from"../lib/server/module-control-service";import{operationsInstance,recordHeartbeat}from"../lib/server/operations-service";loadAuthoritativeEnvironment(process.cwd());
const name="booster-worker",instance=operationsInstance(name),seconds=Math.max(30,Number(process.env.BOOSTER_WORKER_INTERVAL_SECONDS||60));let active:Promise<unknown>|null=null,stopping=false;
async function run(){try{if(await isModulePaused("BOOSTER_WORKER"))return await recordHeartbeat({workerName:name,instanceId:instance,status:"PAUSED",intervalSeconds:seconds});const result=await withBoosterWorkerLock(()=>runBoosterScheduler());await recordHeartbeat({workerName:name,instanceId:instance,status:"IDLE",intervalSeconds:seconds,processed:result?.length||0,metadata:{lockOwned:Boolean(result)}});if(result)process.stdout.write(JSON.stringify({scope:"Booster",processed:result.length})+"\n")}catch(error){await recordHeartbeat({workerName:name,instanceId:instance,status:"FAILED",intervalSeconds:seconds,failed:1,error});console.error(error)}}
async function execute(){if(active||stopping)return;active=run();try{await active}finally{active=null}}
async function main(){await recordHeartbeat({workerName:name,instanceId:instance,status:"STARTING",intervalSeconds:seconds});await execute();const interval=setInterval(()=>void execute(),seconds*1000);
 async function stop(){if(stopping)return;stopping=true;clearInterval(interval);await active?.catch(()=>undefined);await recordHeartbeat({workerName:name,instanceId:instance,status:"STOPPED",intervalSeconds:seconds});await getPool().end().catch(()=>undefined);process.exit(0)}process.once("SIGTERM",()=>void stop());process.once("SIGINT",()=>void stop())}
main().catch(error=>{console.error(error);process.exit(1)});
