const hre=require("hardhat");const{readFile}=require("node:fs/promises");const{resolve}=require("node:path");
async function main(){if(Number((await hre.ethers.provider.getNetwork()).chainId)!==97)throw new Error("Verification is restricted to BSC Testnet (97)");if(!process.env.BSCSCAN_API_KEY)throw new Error("BSCSCAN_API_KEY is required");const d=JSON.parse(await readFile(resolve("deployments","bsc-testnet-x3-aligned.json"),"utf8"));if(d.alignment!=="DIRECT_REFERRAL_X3_V1"||d.rolloutMode!=="TRANSITIONAL")throw new Error("Aligned deployment metadata is invalid");await hre.run("verify:verify",{address:d.address,constructorArguments:[d.usdt,d.genesis,d.deployer,d.treasury,d.authorizer]})}
main().catch(e=>{process.stderr.write(`${e instanceof Error?e.message:"Aligned verification failed"}\n`);process.exitCode=1});
