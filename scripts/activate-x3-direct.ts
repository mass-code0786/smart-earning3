import{transaction,getPool}from"../lib/server/db";
async function main(){const block=Number(process.env.X3_DIRECT_BOUNDARY_BLOCK),log=Number(process.env.X3_DIRECT_BOUNDARY_LOG_INDEX??-1),aligned=(process.env.X3_ALIGNED_CONTRACT_ADDRESS||'').toLowerCase(),configured=(process.env.SMART_EARNING_CONTRACT_ADDRESS||'').toLowerCase();if(!Number.isSafeInteger(block)||block<0||!Number.isInteger(log)||log< -1)throw new Error("Valid X3_DIRECT_BOUNDARY_BLOCK and X3_DIRECT_BOUNDARY_LOG_INDEX are required");if(!aligned||aligned!==configured)throw new Error("Configured contract must equal X3_ALIGNED_CONTRACT_ADDRESS");await transaction(c=>c.query(`UPDATE x3_direct_rollout SET boundary_block_number=$1,boundary_log_index=$2,boundary_contract_event_id=NULL,mode='CONTRACT_ALIGNED',activated_at=now() WHERE singleton=true`,[block,log]));console.log(JSON.stringify({mode:'CONTRACT_ALIGNED',boundaryBlock:block,boundaryLogIndex:log,contract:configured}));}
main().finally(()=>getPool().end());
