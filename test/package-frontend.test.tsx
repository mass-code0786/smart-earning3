import{afterEach,describe,expect,it,vi}from'vitest';import{cleanup,render,screen,waitFor}from'@testing-library/react';import{PackagePage}from'@/components/package-page';
afterEach(()=>{cleanup();vi.restoreAllMocks()});
const packages=[8,16,32,64,128,256,512,1024].map((price,i)=>({packageId:i+1,name:`Package ${i+1}`,priceTokenUnits:String(price*1_000_000),capAdditionTokenUnits:String(price*5_000_000),status:i===0?'PURCHASED':i===1?'AVAILABLE':'LOCKED'}));
describe("package page serial state",()=>{it("shows purchased, one available, and remaining locked from backend state",async()=>{vi.stubGlobal("fetch",vi.fn().mockResolvedValue({ok:true,text:async()=>JSON.stringify({wallet:"0x1",registered:true,nextPackage:2,packages,totalPackageValue:"8000000",registrationValue:"2000000",totalEligibleValue:"10000000",totalEarningCap:"50000000",totalEarned:"40000000",remainingCap:"10000000",cappingStatus:"ACTIVE"})}));render(<PackagePage/>);await waitFor(()=>expect(screen.getByText("5X Earning Cap")).toBeInTheDocument());expect(screen.getByText("Buy $16.00 USDT")).toBeEnabled();expect(screen.getAllByText("Locked")).toHaveLength(6);expect(screen.getAllByText("Purchased").length).toBeGreaterThan(0)})});
