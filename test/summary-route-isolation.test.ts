// @vitest-environment node
import{beforeEach,describe,expect,it,vi}from"vitest";import{NextRequest}from"next/server";
const{requireSession,getUserSummary}=vi.hoisted(()=>({requireSession:vi.fn(),getUserSummary:vi.fn()}));
vi.mock("@/lib/server/auth",()=>({requireSession}));vi.mock("@/lib/server/summary-service",()=>({getUserSummary}));import{GET}from"@/app/api/summary/route";
describe("Summary API ownership",()=>{beforeEach(()=>{vi.clearAllMocks();requireSession.mockResolvedValue({wallet:"0x1234567890abcdef1234567890abcdef12345678"});getUserSummary.mockResolvedValue({items:[],hasMore:false})});it("uses only the authenticated wallet and ignores arbitrary wallet parameters",async()=>{const response=await GET(new NextRequest("http://localhost/api/summary?wallet=0x9999999999999999999999999999999999999999&page=2"));expect(response.status).toBe(200);expect(getUserSummary).toHaveBeenCalledWith("0x1234567890abcdef1234567890abcdef12345678",2)});it("does not read data when authentication fails",async()=>{requireSession.mockRejectedValue(new Error("unauthorized"));await GET(new NextRequest("http://localhost/api/summary"));expect(getUserSummary).not.toHaveBeenCalled()})});
