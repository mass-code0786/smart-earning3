import{cleanup,fireEvent,render,screen}from"@testing-library/react";import{afterEach,describe,expect,it,vi}from"vitest";
vi.mock("@/lib/client/authenticated-fetch",()=>({authenticatedWalletFetch:async()=>new Response(JSON.stringify({magicIncome:"50000",levels:Array.from({length:20},(_,i)=>({level:i+1,requiredDirects:Math.ceil((i+1)/2),qualifiedDirects:1,currentTeam:i?0:1,status:i?"PENDING":"ACHIEVED"}))}),{status:200})}));
import{MagicLevelReport}from"@/components/magic-level-report";afterEach(cleanup);
describe("Magic Level report UI",()=>{it("paginates levels 1-10 and 11-20 and shows real income",async()=>{render(<MagicLevelReport/>);expect(await screen.findByText("$0.05 USDT")).toBeInTheDocument();expect(screen.getByText("Achieved")).toBeInTheDocument();expect(screen.getByText("Level 10")).toBeInTheDocument();expect(screen.queryByText("Level 11")).not.toBeInTheDocument();fireEvent.click(screen.getByRole("button",{name:/11/}));expect(screen.getByText("Level 20")).toBeInTheDocument()})});
