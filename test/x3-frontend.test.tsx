import{afterEach,describe,expect,it,vi}from"vitest";import{cleanup,render,screen,waitFor}from"@testing-library/react";import{X3Page}from"@/components/x3-page";
afterEach(()=>{cleanup();vi.restoreAllMocks()});
describe("package X3 frontend",()=>{it("shows held income as non-withdrawable with activation guidance",async()=>{vi.stubGlobal("fetch",vi.fn().mockResolvedValue({ok:true,json:async()=>({packages:[{packageId:2,priceTokenUnits:"16000000",x3Allocation:"4000000",active:false,permanentSponsor:"0x1",matrixParent:null,currentCycle:0,slots:[],earnedIncome:"0",heldIncome:"4000000",releasedIncome:"0",recycleCount:0}]})}));render(<X3Page/>);await waitFor(()=>expect(screen.getByText("Activate this package to release your held X3 income.")).toBeInTheDocument());expect(screen.getAllByText("$4.00")).toHaveLength(2);expect(screen.getByText("Upgrade / Buy Package")).toBeInTheDocument()})});
